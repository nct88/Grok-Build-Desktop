# Grok Build Desktop 0.5.48

<!-- release:vi -->
<!-- release:en -->

| Tiếng Việt | English |
|---|---|
| Bản 0.5.48 sửa lỗi `ReferenceError: sup is not defined` trong `agent:connect` và hoàn thiện luồng đồng bộ quyền thời gian thực. | Release 0.5.48 fixes `ReferenceError: sup is not defined` in `agent:connect` and completes real-time permission sync. |

## Nội dung phát hành / Release highlights

| Tiếng Việt | English |
|---|---|
| Khắc phục lỗi `ReferenceError: sup is not defined` xảy ra khi khởi tạo kết nối agent host trong `main.cjs`. | Fixed `ReferenceError: sup is not defined` when initializing agent host connection in `main.cjs`. |
| Đảm bảo `connectAgentHost` khởi tạo đúng `supervisor` và handler phân quyền. | Guaranteed proper `supervisor` and permission handler initialization in `connectAgentHost`. |

## Gói phát hành / Release artifacts

- `Grok-Build-Setup-0.5.48.exe` — NSIS Setup / Bộ cài NSIS
- `Grok-Build-0.5.48-win32-x64-portable.exe` — Portable executable / Bản portable EXE
- `Grok-Build-0.5.48-win32-x64.zip` — Portable ZIP / Bản ZIP giải nén
- `MANIFEST.json` — Sizes and SHA-256 / Kích thước và SHA-256

| Tiếng Việt | English |
|---|---|
| Các tệp Windows chưa được ký Authenticode và có thể kích hoạt cảnh báo SmartScreen. Hãy kiểm tra SHA-256 trong `MANIFEST.json` trước khi chạy. Bản này là ứng viên local tại `dist/0.5.48/` cho đến khi phát hành GitHub. | The Windows artifacts are not Authenticode-signed and may trigger SmartScreen. Verify SHA-256 values in `MANIFEST.json` before running them. This is a local candidate under `dist/0.5.48/` until a GitHub release is published. |

## Kiểm chứng / Verification

| Tiếng Việt | English |
|---|---|
| Trước đóng gói: e2e 30 passed, ACP client connect OK, agent host supervisor lifecycle OK. | Before packaging: e2e 30 passed, ACP client connect OK, agent host supervisor lifecycle OK. |
| `scripts/publish-release.ps1 -Version 0.5.48` tạo Setup, portable EXE/ZIP, update pack và MANIFEST. | `scripts/publish-release.ps1 -Version 0.5.48` produces Setup, portable EXE/ZIP, update pack and MANIFEST. |
